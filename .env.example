# URL de l'API backend (gestion-locative-api).
# Dev : back lancé localement (pnpm dev dans le repo gestion-locative-api)
# Prod : https://api.gestion-locative.zeleph.fr
VITE_API_URL=http://localhost:3000
